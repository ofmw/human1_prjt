<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.omart.OrderMapper">    
    
    <insert id="createOrder" parameterType="orderVo">
        insert into order_list (order_idx, m_idx, products, amounts, products_price, receiver, selnum, address, paid_price) 
        values (#{order_idx}, #{m_idx}, #{products}, #{amounts}, #{products_price}, #{receiver}, #{selnum}, #{address}, #{paid_price})
    </insert>
    
    <select id="selectOrder" parameterType="string" resultType="orderVo">
        select * from order_list where order_idx = #{orderNum}
    </select>
    
</mapper>